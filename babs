<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Valentine 🌹❤️</title>

  <style>
    * { box-sizing: border-box; }

    body {
      margin: 0;
      height: 100vh;
      overflow: hidden;
      background: linear-gradient(135deg, #8b0000, #c1121f, #e63946);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Georgia', serif;
    }

    /* Floating roses */
    .rose {
      position: absolute;
      font-size: 22px;
      opacity: 0.8;
      animation: floatUp 9s linear infinite;
    }

    @keyframes floatUp {
      0% { transform: translateY(100vh) scale(1); opacity: 0; }
      20% { opacity: 1; }
      100% { transform: translateY(-10vh) scale(1.5); opacity: 0; }
    }

    .card, .final {
      background: rgba(255,255,255,0.95);
      padding: 50px;
      border-radius: 30px;
      text-align: center;
      box-shadow: 0 25px 50px rgba(0,0,0,0.3);
      max-width: 440px;
      animation: fadeIn 1.5s ease;
      z-index: 10;
    }

    .final { display: none; }

    h1 {
      color: #a4161a;
      font-size: 28px;
      margin-bottom: 20px;
    }

    p {
      color: #444;
      font-size: 17px;
      line-height: 1.6;
      margin-bottom: 30px;
    }

    .buttons {
      display: flex;
      justify-content: center;
      gap: 25px;
    }

    button {
      padding: 14px 36px;
      border: none;
      border-radius: 40px;
      font-size: 16px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .yes {
      background: linear-gradient(135deg, #e63946, #a4161a);
      color: white;
      box-shadow: 0 10px 20px rgba(230,57,70,0.5);
    }

    .yes:hover {
      transform: scale(1.15);
    }

    .no {
      background: #dee2e6;
      color: #555;
      position: relative;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>

<script>
  function createRose() {
    const rose = document.createElement("div");
    rose.className = "rose";
    rose.innerHTML = "🌹";
    rose.style.left = Math.random() * 100 + "vw";
    rose.style.fontSize = Math.random() * 18 + 16 + "px";
    rose.style.animationDuration = Math.random() * 4 + 6 + "s";
    document.body.appendChild(rose);
    setTimeout(() => rose.remove(), 9000);
  }
  setInterval(createRose, 300);
</script>

<!-- First Screen -->
<div class="card" id="card">
  <h1>Will you be my Valentine, mama? 🌹❤️</h1>
  <p>
    Every moment with you feels like a blessing ✨<br>
    This Valentine’s Day wouldn’t mean anything without you.
  </p>

  <div class="buttons">
    <button class="yes" onclick="showFinal()">Yes 💖</button>
    <button class="no" onmouseover="moveNo()">No 🙈</button>
  </div>
</div>

<!-- Final Screen -->
<div class="final" id="final">
  <h1>You said yes 🌹💖</h1>
  <p>
    You are my world, my heart, my everything ❤️<br>
    I love you more than words can ever explain.<br><br>
    Thank you for choosing me — today, tomorrow, and always 💐✨
  </p>
  <p style="font-size:22px;">🌹❤️💍💖</p>
</div>

<script>
  function showFinal() {
    document.getElementById("card").style.display = "none";
    document.getElementById("final").style.display = "block";
  }

  function moveNo() {
    const noBtn = document.querySelector(".no");
    const x = Math.random() * 260 - 130;
    const y = Math.random() * 260 - 130;
    noBtn.style.transform = `translate(${x}px, ${y}px)`;
  }
</script>

</body>
</html>
